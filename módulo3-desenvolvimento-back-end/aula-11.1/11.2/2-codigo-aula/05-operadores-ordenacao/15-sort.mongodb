// Executar uma ordenação de todos os documentos da coleção filmes pela ordem decrescente do ano e anti alfabética

use("aula-filter-operators");

db.filmes.find().sort({ "titulo": 1 })

// db.filmes.find().sort({ "ano": -1 }, { "titulo": -1 })